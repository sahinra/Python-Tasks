# Design a simple command-line To-Do list where users can add tasks, mark them as done,
# and list all tasks. (Advanced: Save tasks to a file, so they persist between sessions.)

def create_task(task):
    return f"{task}\n"

